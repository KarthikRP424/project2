package com.posture.companion.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Palette ──────────────────────────────────────────────────────────
val Blue400   = Color(0xFF4f8ef7)
val Blue300   = Color(0xFF7aabff)
val Blue200   = Color(0xFFa8c8ff)
val BlueGlow  = Color(0x334f8ef7)   // semi-transparent glow

// ─── Backgrounds ──────────────────────────────────────────────────────────────
val Background   = Color(0xFF0d1117)   // GitHub dark background
val Surface      = Color(0xFF161b22)   // GitHub dark surface
val SurfaceVar   = Color(0xFF21262d)   // slightly lighter surface
val SurfaceHigh  = Color(0xFF30363d)   // card border / elevated

// ─── Status Colours ───────────────────────────────────────────────────────────
val GreenConnected    = Color(0xFF3fb950)   // GitHub green
val GreenConnectedDim = Color(0x223fb950)
val RedError          = Color(0xFFf85149)   // GitHub red
val RedErrorDim       = Color(0x22f85149)
val AmberConnecting   = Color(0xFFd29922)   // GitHub yellow
val AmberConnectingDim= Color(0x22d29922)

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFe6edf3)
val TextSecondary = Color(0xFF8b949e)
val TextMuted     = Color(0xFF484f58)

// ─── Accent / Alerts ──────────────────────────────────────────────────────────
val AccentPurple = Color(0xFFbc8cff)
val AccentOrange = Color(0xFFffa657)

// ─── On-Color helpers ─────────────────────────────────────────────────────────
val OnPrimary  = Color(0xFF0d1117)
val OnSurface  = TextPrimary
